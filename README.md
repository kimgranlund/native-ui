# native-ui
Zero-dependency web component library with an optional OKLCH CSS design system. Composable trait controllers, signal-based reactivity, no shadow DOM. Styling via CSS custom property inheritance, light-dark() theming, and zero-specificity attribute selectors.
